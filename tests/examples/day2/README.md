# Day 2 – AI Extensions + CI/CD

## Part 1 – AI-powered edge cases
1. **Review AI-generated specs** (`../day1/login-edge-cases.spec.ts`) and add missing assertions/data (focus on edge cases like locked/performance users).
2. **Promotion checklist** – decide which cases move into the main suite.
3. **Trace/transcript audit** – align MCP outputs (from Day 1) with the generated code.

## Part 2 – Playwright agents (chat-driven)
1. **Agents overview** (`agents.md`) – planner/generator/healer roles, guardrails, telemetry.
2. **VS Code setup verification** – ensure VS Code Insiders and agent definitions are ready.
3. **Live session** – use VS Code Insiders to chat with the agents and produce a new plan/spec.
4. **Healer experiment** – break a test, let the healer suggest fixes, discuss when to accept/reject.
5. **AI-generated specs**:
   - `checkout-happy-path.spec.ts` – example of checkout flow test generated by Playwright agents.

## Part 3 – CI/CD + reporting
1. **CI/CD** – walk through `.github/workflows/playwright.yml`, artifact uploads, and GitHub Job Summary.
2. **Docker + reproducibility** – reiterate when to run `npm run docker:test`.
