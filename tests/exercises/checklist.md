# Facilitator Checklist

- [ ] Clone repo + install deps (`npm install`, `npm run playwright:install`)
- [ ] Verify `npm run mcp:demo` produces transcript, screenshot, trace
- [ ] Demo `npm run docker:test` on at least one machine
- [ ] Walk through `tests/examples/day1` prompt doc before the session
- [ ] Prepare `.env` file example for dotenv demo (create `.env` with `USER_NAME=standard_user` and `PASSWORD=secret_sauce`)
- [ ] Validate GitHub Actions run succeeded (artifacts uploaded)
- [ ] Build slides (`npm run slides:build`) and share HTML/PDF export
- [ ] Prepare clean "step-by-step" branch for live coding
